import Link from 'next/link'; import { requireUser } from '@/lib/auth';
export default async function Integrations(){const u=await requireUser();return <div className="content"><div className="eyebrow">Live connections</div><h1 className="h1">Integrations</h1><p className="muted">Connect your own services. Credentials are not hardcoded into Careerly and provider secrets stay server-side.</p><div className="grid grid2" style={{marginTop:16}}><div className="card"><div className="split"><div><div className="job-title">Google</div><div className="small muted">Gmail + Calendar</div></div><span className="pill">OAuth</span></div><p className="small muted">Use your Google account to read recruiter mail with authorized scopes, send mail, and create interview events.</p><a className="btn" href="/api/integrations/google/start">Connect Google</a></div><div className="card"><div className="job-title">Greenhouse ingestion</div><p className="small muted">Enter a public Greenhouse board token to pull live published jobs. Application submission requires an employer-authorized application credential.</p><form action="/api/sources/greenhouse" method="post"><label className="label" htmlFor="greenhouse-board-token">Board token</label><input id="greenhouse-board-token" className="input" name="boardToken" placeholder="Board token"/><label className="label" htmlFor="greenhouse-company" style={{marginTop:8}}>Company</label><input id="greenhouse-company" className="input" name="company" placeholder="Company" style={{marginTop:4}}/><button className="btn" style={{marginTop:8}}>Use live source</button></form></div><div className="card"><div className="job-title">Lever ingestion</div><p className="small muted">Pull live published postings from Lever's public posting feed using the employer slug.</p><form action="/api/sources/lever" method="post"><label className="label" htmlFor="lever-company">Employer slug</label><input id="lever-company" className="input" name="company" placeholder="Employer slug"/><button className="btn" style={{marginTop:8}}>Use live source</button></form></div><div className="card"><div className="job-title">Security</div><p className="small muted">Two-factor authentication is configured from Security. Use an authenticator app QR scan and verify the six-digit code.</p><Link className="btn" href="/security">Open Security</Link></div></div></div>}
