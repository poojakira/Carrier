'use client'; import { useEffect,useState } from 'react';
export default function Applications(){const [apps,setApps]=useState<any[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{fetch('/api/applications').then(r=>r.json()).then(data=>{setApps(data);setLoading(false);}).catch(()=>{setError('Failed to load applications.');setLoading(false);})},[]);if(loading)return <div className="content"><div className="card" style={{textAlign:'center',padding:32}}>Loading...</div></div>;if(error)return <div className="content"><div className="card" style={{textAlign:'center',padding:32,color:'var(--danger)'}}>{error}</div></div>;const counts=apps.reduce((a,x)=>(a[x.status]=(a[x.status]||0)+1,a),{} as Record<string,number>);return <div className="content"><div className="topbar"><div><div className="eyebrow">Track</div><h1 className="h1">Application CRM</h1><div className="muted">Every submission carries its job snapshot, policy decision, content versions, and status history.</div></div></div><div className="grid grid4">{['READY','SUBMITTED','SCREEN','INTERVIEW'].map(s=><div className="card" key={s}><div className="kicker">{s}</div><div className="metric">{counts[s]||0}</div></div>)}</div><div className="card" style={{marginTop:16,overflowX:'auto'}}><table className="table"><thead><tr><th>Role</th><th>Company</th><th>Match</th><th>Status</th><th>Adapter</th><th>Resume</th><th>Created</th></tr></thead><tbody>{apps.map(a=><tr key={a.id}><td><strong>{a.job.title}</strong></td><td>{a.job.company}</td><td><span className="pill good">{a.job.overallFit}%</span></td><td><span className="pill">{a.status}</span></td><td>{a.adapter}</td><td>{a.resumeVersion}</td><td className="small muted">{new Date(a.createdAt).toLocaleString()}</td></tr>)}</tbody></table></div></div>}
