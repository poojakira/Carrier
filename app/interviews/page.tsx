import { requireUser } from '@/lib/auth'; import { db } from '@/lib/db';
export default async function Interviews(){const u=await requireUser();const apps=await db.application.findMany({where:{userId:u.id,status:{in:['INTERVIEW','OFFER']}},include:{job:true},orderBy:{updatedAt:'desc'}});return <div className="content"><div className="eyebrow">Interview OS</div><h1 className="h1">Interview Center</h1><p className="muted">Briefings, mock interviews, STAR stories, role-specific technical preparation, and post-interview learning.</p><div className="grid grid2" style={{marginTop:16}}>{apps.length?apps.map(a=><div className="card" key={a.id}><div className="pill good">{a.status}</div><h2>{a.job.title}</h2><div className="muted">{a.job.company}</div><hr style={{borderColor:'var(--line)',borderWidth:1,margin:'18px 0'}}/><div className="stack">{['Company briefing','Role briefing','Likely technical topics','Behavioral questions','Relevant projects','STAR stories','Questions to ask'].map(x=><div className="split" key={x}><span>{x}</span><button className="btn small muted">Generate →</button></div>)}</div></div>):<div className="card"><h3>No interviews yet</h3><p className="muted">When an application moves to Interview, this workspace activates.</p></div>}</div></div>}
