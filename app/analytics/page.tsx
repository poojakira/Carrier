'use client'; import { useEffect,useState } from 'react';
export default function Analytics(){const [a,setA]=useState<any>(null);useEffect(()=>{fetch('/api/analytics').then(r=>r.json()).then(setA)},[]);if(!a)return <div className="content"><div className="card">Loading analytics…</div></div>;const values=[32,44,58,68,74,85,92];return <div className="content"><div className="eyebrow">Optimize</div><h1 className="h1">Career analytics</h1><p className="muted">Apply → Measure → Learn → Improve → Apply better.</p><div className="grid grid4"><div className="card"><div className="kicker">Jobs found</div><div className="metric">{a.found}</div></div><div className="card"><div className="kicker">Applications</div><div className="metric">{a.apps}</div></div><div className="card"><div className="kicker">Interview rate</div><div className="metric good">{a.screenRate}%</div></div><div className="card"><div className="kicker">Offers</div><div className="metric good">{a.offers}</div></div></div><div className="grid grid2" style={{marginTop:16}}><div className="card"><div className="kicker">Illustrative trend</div><h3>Application quality signal</h3><div className="chart">{values.map((v,i)=><div className="bar" key={i} style={{height:`${v}%`}} />)}</div><div className="bar-labels">{values.map((_,i)=><div key={i}>W{i+1}</div>)}</div></div><div className="card"><div className="kicker">Next learning loop</div><h3>Skill gap analysis</h3><ul className="list"><li><div className="split"><span>Kubernetes security</span><span className="pill warn">high impact</span></div></li><li><div className="split"><span>AI agent threat modeling</span><span className="pill good">strong</span></div></li><li><div className="split"><span>Cloud IAM</span><span className="pill">developing</span></div></li></ul></div></div></div>}
